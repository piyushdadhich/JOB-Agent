"""job-agent — local-first AI job search agent.

Top-level package for the `job-agent` CLI. The actual pipeline
code lives in sibling packages (engine/, agents/, skills/,
dashboard/) — this package only hosts the user-facing entry
points wired up in jobagent/cli.py.
"""

__version__ = "0.1.0"
